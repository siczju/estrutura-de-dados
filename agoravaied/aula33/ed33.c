//       Implementando um TAD

/*

    Crie um TAD de um vetor de float:
     -> O vetor tem uma capacidade máxima(número máximo de elementos);
     -> O vetor informa seu tamanho (quantidade de elementos armazenados atualmente)

    Funções:
    * size(tad vector) -> retorna o tamanho do vetor(número atual de elementos inseridos)
    * capacity(tad vector) -> retorna a capacidade do vetor(número máximo de elementos)
    * at(tad vector, int index) -> ele vai checar se esse indice é valido ou não se está dentro do range de elementos disponíveis
    * get(tad vector, int index) -> mesma coisa do at, porém se passar um indice invalido ele vai rodar msm assim e vai dar erro
    * append(tad vector, float val) -> adiciona o valor 'val' no final do vetor. Lança um erro se o vetor estive cheio
    * set(tad vector, int index, float val) -> atribui o valor 'val no índice 'index' do vetor de tipo 'tad'.Lança um erro se indice é invalido
    * print(tad vector) -> imprime todos os elementos do vetor.
    
    
*/

#include <stdio.h>
#include <stdlib.h>

void main(){



}